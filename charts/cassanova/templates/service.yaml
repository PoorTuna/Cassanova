apiVersion: v1
kind: Service
metadata:
  name: {{ include "cassanova.fullname" . }}
  labels:
    {{- include "cassanova.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cassanova.selectorLabels" . | nindent 4 }}
